apply plugin: 'build-dashboard'

task wrapper(type: Wrapper) {
    gradleVersion = '2.13'
}

buildscript {
  repositories {
    maven {
      url "https://plugins.gradle.org/m2/"
    }
  }
	dependencies {
    	classpath "gradle.plugin.com.palantir.gradle.docker:gradle-docker:0.7.2"
		//classpath "net.researchgate:gradle-release:2.4.0"
		# and look at 'http://stackoverflow.com/questions/32650444/how-to-install-or-publish-submodules-of-gradle-project-as-a-jar'
		classpath "org.robovm:robovm-gradle-plugin:1.5.0"
  	}
}

// apply plugins
//apply plugin: "com.palantir.docker"
//apply plugin: "com.palantir.docker-run"
//apply plugin: "java"
//apply plugin: "net.researchgate.release"

// configure dependencies
apply from: 'buildSrc/dependencies.gradle'
apply from: 'buildSrc/build-tasks.gradle'
